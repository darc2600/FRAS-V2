# API Test Report

Generated: 2026-05-04

## Summary
- OpenAPI reachable at `/openapi.json`.
- Superadmin login succeeded and token obtained.
- GET endpoints: 23 tested; most public GETs returned 200.
- Admin GETs return 403 without auth and 200 with superadmin auth.
- POST/PUT/DELETE endpoints: several tested safely (create/delete user, support ticket flows, room schedule create/delete).

## Tested endpoints (GET)
- /api/rooms — 200 (items: 6)
- /api/floors — 200 (items: 4)
- /api/attendance — 422 (requires query/body)
- /api/courses — 200 (items: 13)
- /api/instructors — 200 (items: 21)
- /api/students — 200 (items: 150)
- /api/classes — 200 (items: 6)
- /test — 200
- /api/debug/face-embedding-coverage-public — 200 (keys present)
- Admin GETs tested unauthenticated: returned 403; with auth returned 200 (e.g., `/api/admin/users`, `/api/admin/analytics`, `/api/admin/support/tickets`).

## Tested write flows (safe)
- Create + Delete user (`POST /api/admin/users`, `DELETE /api/admin/users/{id}`)
  - Created user: returned 200 and `user_id`.
  - Deleted user: returned 200 and confirmation.
  - See `tools/smoke_test_post_results.json`.

- Support ticket flow
  - `POST /api/support/tickets` — created ticket (200) with `ticket_id`.
  - `PUT /api/admin/support/tickets/{ticket_id}` — updated (200).
  - `POST /api/admin/support/tickets/{ticket_id}/replies` — reply added (200).
  - See `tools/smoke_test_write_extended_results.json`.

- Room schedule
  - `POST /api/room-schedule/{room_code}` — created schedule entry (200) using valid instructor `Brown, Michael` and course `CIS101`.
  - `DELETE /api/room-schedule/{room_code}` — deleted schedule (200).
  - See `tools/smoke_test_write_extended_results.json`.

## Untested or partially-tested endpoints
- `/api/attendance` (GET returned 422; needs proper query params). Consider testing with valid `course_code` and `section`.
- File upload endpoints:
  - `/api/capture` (POST) — not tested (requires multipart file upload).
  - `/api/registration` (POST) — not tested (multipart/form-data with images).
- Recognition endpoints:
  - `/api/recognize` (POST) — not tested (image / payload required).
- Bulk and admin write endpoints (PUT/POST with complex payloads):
  - `/api/admin/users/bulk` — not tested.
  - `/api/admin/attendance/export` — not tested.
  - `/api/admin/system-settings` (PUT) — not tested.

## Test artifacts
- tools/smoke_test_extended_results.json — GET coverage and details
- tools/smoke_test_post_results.json — user create/delete
- tools/smoke_test_write_extended_results.json — support ticket and room schedule writes

## Recommendations / Next steps
1. Test `/api/attendance` with valid `course_code` and `section` to verify attendance retrieval.
2. Test file-upload flows (`/api/capture`, `/api/registration`, `/api/recognize`) using small sample images.
3. Add CI-friendly smoke-test runner that fails on non-2xx for endpoints expected to be operational.
4. Optionally, expand tests to verify response shapes (schemas) against OpenAPI.

---

*Generated by automated smoke-test scripts in `tools/`.*
