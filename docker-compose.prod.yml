services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - SQLITE_DB_PATH=/app/data/attendance.db
      - DATASET_PATH=/app/dataset
    volumes:
      - ./data:/app/data
      # mount a host dataset directory (writable) and the repo into the container
      - ./data/dataset:/app/dataset
      - ./:/app
    ports:
      - "8000:8000"

  frontend:
    image: nginx:stable-alpine
    restart: unless-stopped
    volumes:
      - ./facial-attendance/dist/facial-attendance:/usr/share/nginx/html:ro
    ports:
      - "8080:80"
